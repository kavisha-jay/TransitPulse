[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests for modular functions
    integration: Integration tests requiring external services or docker containers
    spark: PySpark job and DataFrame transformation tests
    data_quality: Data contract and validation tests
